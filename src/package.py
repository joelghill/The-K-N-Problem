# Joel Hill
# jgh719
# 10344729

# Class to represent package
import networkx as nx
import matplotlib.pyplot as plt

class Package(object):

	dropoff = None
	pickup	= None
	map 

	"""Package contains a pickup and dropoff location"""
	def __init__(self, pickup = None, dropoff = None, map = None):
		super(Package, self).__init__()
		
		if(pickup == None and dropoff == None and graph != None):
			#generate random points from graph
		else if (pickup)
		